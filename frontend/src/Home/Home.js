import React from 'react';

// Components
import Navbar from '../Components/Navbar';

const Home = props => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default Home;